@model IEnumerable<CRM.Models.VProductosMasCotizado>

@{
    ViewData["Title"] = "Index";
}

<h1>Productos más cotizados</h1>

<div class="container">
    <form class="form" method="POST">
        <div>
            <label>Fecha inicio</label>
            <input type="date" id="inicio" name="inicio" />
        </div>
        <div>
            <label>Fecha fin</label>
            <input type="date" id="fin" name="fin" />
        </div>
        <button class="btn btn-success" id="form-btn" asp-controller="VProductosMasCotizados" asp-action="Filtrar">Filtrar</button>
    </form>
</div>

<table class="table th-inner sortable both"
       id="table"
       data-toggle="table"
       data-search="false"
       data-show-columns="false"
       data-show-multi-sort="true"
       data-pagination="true"
>
    <thead>
        <tr>
            <th data-field="Descripcion" data-sortable="true">
                @Html.DisplayNameFor(model => model.Descripcion)
            </th>
            <th data-field="CantidadCotizaciones" data-sortable="true">
                @Html.DisplayNameFor(model => model.CantidadCotizaciones)
            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Descripcion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CantidadCotizaciones)
            </td>
        </tr>
}
    </tbody>
</table>
