@model IEnumerable<CRM.Models.VTop10CotizacionDiferenciaMasAlto>

@{
    ViewData["Title"] = "Index";
}

<h1>Top 10 cotizaciones con mayor diferencia</h1>


<div class="container">
    <form class="form" method="POST">
        <div>
            <label>Fecha inicio</label>
            <input type="date" id="inicio" name="inicio" />
        </div>
        <div>
            <label>Fecha fin</label>
            <input type="date" id="fin" name="fin" />
        </div>
        <button class="btn btn-success" id="form-btn" asp-controller="VTop10CotizacionDiferenciaMasAlto" asp-action="Filtrar">Filtrar</button>
        <button class="btn btn-secondary" id="form-btn" asp-controller="VTop10CotizacionDiferenciaMasAlto" asp-action="Index">Quitar filtro</button>
    </form>
</div>

<table class="table th-inner sortable both"
       id="table"
       data-toggle="table"
       data-search="false"
       data-show-columns="false"
       data-show-multi-sort="true"
       data-sort-priority='[{"sortName": "NumeroCotizacion","sortOrder":"desc"},{"sortName":"Nombre","sortOrder":"desc"},{"sortName":"Diferencia","sortOrder":"desc"}]'>
    <thead>
        <tr>
            <th data-field="Diferencia" data-sortable="true">Diferencia</th>
            <th data-field="Nombre" data-sortable="true">Nombre</th>
            <th data-field="NumeroCotizacion" data-sortable="true">NumeroCotizacion</th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.NumeroCotizacion)
                </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Diferencia)
            </td>
            
        </tr>
}
    </tbody>
</table>
