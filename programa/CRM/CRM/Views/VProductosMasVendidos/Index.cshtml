@model IEnumerable<CRM.Models.VProductosMasVendido>

@{
    ViewData["Title"] = "Index";
}

<h1>Productos mas vendidos</h1>


<div class="container">
    <form class="form" method="get">
        <div>
            <label>Fecha inicio</label>
            <input type="date" id="inicio" name="inicio" />
        </div>
        <div>
            <label>Fecha fin</label>
            <input type="date" id="fin" name="fin" />
        </div>
        <button class="btn btn-success" id="form-btn" asp-controller="VProductosMasVendidos" asp-action="Filtrar">Filtrar</button>
        <button class="btn btn-secondary" id="form-btn" asp-controller="VProductosMasVendidos" asp-action="Index">Quitar filtro</button>
    </form>
</div>

<table class="table th-inner sortable both"
       id="table"
       data-toggle="table"
       data-search="false"
       data-show-columns="false"
       data-show-multi-sort="true"
       data-sort-priority='[{"sortName": "Monto","sortOrder":"desc"},{"sortName":"Descripcion","sortOrder":"desc"}]'>
    <thead>
        <tr>
            <th data-field="Monto" data-sortable="true">Monto</th>
            <th data-field="Descripcion" data-sortable="true">Descripcion</th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Monto)
            </td>
             <td>
                @Html.DisplayFor(modelItem => item.Descripcion)
             </td>
                
        </tr>
}
    </tbody>
</table>
